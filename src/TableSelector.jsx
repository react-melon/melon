/**
 * @file melon/TableSelector
 * @author leon(ludafa@outlook.com)
 */

var React = require('react');

function createSelector(
    cellData, cellDataKey,
    rowData, rowIndex,
    columnData, columnIndex,
    width
) {
    return (
        <input type="checkbox" />
    );
};

module.exports = createSelector;
